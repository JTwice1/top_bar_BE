export type Entry = {
  desktop: string;
  mobile: string;
  desktopBtn: string;
  mobileBtn: string;
  desktopBtnHref: string;
  mobileBtnHref: string;
  startDate: string; // ISO 8601 format
  endDate: string; // ISO 8601 format
};
